import React from 'react';
export const BetChart = ({ classAlt }) => {
  return (
    <div
      className={`bg-gray-100 rounded-3xl flex items-center justify-center text-white ${classAlt}`}
    >
      Chart here
    </div>
  );
};
