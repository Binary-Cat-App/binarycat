import React from 'react';
export const BetChart = ({ classAlt }) => {
  return (
    <div
      className={`bg-gray-100 rounded flex items-center justify-center text-white ${classAlt}`}
    >
      Chart here
    </div>
  );
};
